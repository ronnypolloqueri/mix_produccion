<h1>Lista de Archivos</h1>
<table class="table table-striped">
	<thead>
		<tr>
			<th>Nombre archivo</th>
			<th>Fecha creación</th>
			<th></th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		<% @archivos.each do |archivo|%>
			<tr>
				<td><%= archivo.nombre.capitalize %></td>
				<td><%= archivo.created_at.strftime("%m/%d/%Y %T %p") %></td>
				<td><%= link_to 'Mostrar', ver_path(archivo.id) %></td>
				<td><%= link_to 'Reporte', costos_totales_path(archivo.id) %></td>
				<td><%= link_to 'Eliminar', eliminar_archivo_path(archivo.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
			</tr>
		<% end %>
	</tbody>
</table>

<%= link_to 'Nuevo Archivo', nuevo_archivo_path %>